# ─────────────────────────────────────────────────────────────────────────────
# YALNIZCA LOKAL GELİŞTİRME profili. Çalıştırma: SPRING_PROFILES_ACTIVE=local
# Bu değerler localhost dışında erişilemez ve PROD'da KULLANILMAZ.
# Prod'da tüm secret'lar env değişkeninden (docker-compose .env) gelir.
# ─────────────────────────────────────────────────────────────────────────────
spring.datasource.password=${DB_PASSWORD:demir}
keycloak.admin.password=${KEYCLOAK_ADMIN_PASSWORD:admin}
# Lokal dev için dummy şifreleme anahtarı (gerçek veri korumaz).
encryption.secret=${ENCRYPTION_SECRET:LocalDevOnlySecretKey!}
encryption.salt=${ENCRYPTION_SALT:a1b2c3d4e5f60718}
# Finnhub için lokal anahtarını env ile ver; yoksa piyasa verisi çekilmez.
finnhub.api.key=${FINNHUB_API_KEY:}
